const fs = require("fs");
const path = require("path");

const sections = {
  "css-color-gradient-guide": '<h2>Gradient Performance and Browser Compatibility</h2><p>CSS gradients are rendered by the browser engine, not loaded as images. This makes them resolution-independent — they look sharp on any screen density without additional HTTP requests. However, complex gradients with many color stops can impact rendering performance on low-powered mobile devices. Each color stop requires the browser to calculate a color transition, and animating gradients (changing the angle, shifting color stops) triggers repaints. For performance-critical pages, limit gradients to two or three stops and avoid animating them on elements that cover large portions of the viewport.</p><p>Browser support for CSS gradients is universal in modern browsers. Linear gradients have been supported since IE10. Radial gradients since IE10 with the -ms- prefix. The modern syntax (to right, at center) has been supported everywhere since roughly 2015. If you need to support very old browsers, provide a solid background-color fallback before the gradient declaration. Browsers that do not understand gradients will ignore the declaration and use the fallback.</p>',

  "csv-json-conversion-guide": '<h2>Choosing Between CSV and JSON for Your Project</h2><p>CSV excels when your data is tabular, your users work with spreadsheets, and human readability in text editors matters. JSON excels when your data has hierarchy, you are communicating between programs via APIs, and type fidelity matters. The right choice depends on your data shape and your consumers. A common anti-pattern: forcing deeply nested JSON into a CSV by flattening keys. This works technically but produces CSVs with dozens of columns that are hard to work with in Excel. If your data is hierarchical, keep it in JSON and provide a CSV export that summarizes or extracts a subset.</p><p>For data exchange between systems you control, consider formats beyond CSV and JSON. Parquet and Avro provide compression and schema evolution. Protocol Buffers and MessagePack provide compact binary serialization. CSV and JSON are the simplest, most universal options — they are human-readable and require no tooling beyond a text editor — but they are not always the most efficient.</p>',

  "line-deduplication-guide": '<h2>Fuzzy Deduplication and Record Linkage</h2><p>Exact deduplication finds identical lines. It does not find near-duplicates: "John Smith" and "John  Smith" (double space), "example.com" and "Example.Com" (different case), or "Acme Corp" and "Acme Corporation" (different forms). For these, you need fuzzy matching. Edit distance algorithms (Levenshtein, Damerau-Levenshtein) measure how many character changes separate two strings. SimHash and MinHash identify near-duplicate documents by comparing hash signatures. These techniques are more complex than exact matching but catch duplicates that would otherwise go undetected.</p><p>Record linkage is the broader problem: given two datasets with no shared identifier, find which records refer to the same entity. A CRM might have a contact listed one way and an email list listing them another way — same person, different representation. Probabilistic record linkage uses multiple fields (name, email, address, phone) to estimate match probability. This is what deduplication tools in CRMs and marketing platforms do under the hood.</p>',

  "naming-conventions-guide": '<h2>Enforcing Naming Conventions in a Team</h2><p>The best naming convention is the one your team actually follows. A documented convention that nobody enforces is worse than no convention at all — it creates inconsistency, which is more confusing than consistently "wrong" naming. Enforce conventions with automated tooling: ESLint rules for JavaScript (camelcase, @typescript-eslint/naming-convention), Pylint or Black for Python (snake_case is the default), Stylelint for CSS (kebab-case), and Prettier for general formatting. If your linter catches naming violations before code review, developers fix them immediately rather than debating them in PR comments.</p><p>When onboarding new team members, point them to your naming convention documentation and your linter config. Do not rely on them "picking up" the conventions by reading existing code — different parts of a legacy codebase may follow different conventions, and a new developer cannot distinguish "this is our convention" from "this file was written before we adopted conventions." Explicit documentation plus automated enforcement removes the ambiguity.</p>',

  "password-hash-guide": '<h2>Handling Password Resets Securely</h2><p>Password reset flows are among the most attacked features in any application. The key principles: reset tokens must be random (generated with crypto.randomBytes, not Math.random), single-use (invalidated after use), and time-limited (expire after 15-60 minutes). Store only a SHA-256 hash of the token in the database — the same way you store password hashes. If the database is breached, attackers cannot use the hashed tokens to reset passwords because they would need to reverse the hash to find the original token.</p><p>Never tell the user whether an account exists during login or password reset. A message saying "If an account with that email exists, we have sent a reset link" prevents attackers from enumerating your user base. The reset email should contain a link with the token in the URL (over HTTPS, so the token is encrypted in transit). After the user sets a new password, invalidate all existing sessions — this prevents an attacker who gained access to the account from maintaining access after the password is changed.</p>',

  "text-processing-guide": '<h2>Building Reusable Text Processing Scripts</h2><p>The tools on this site solve ad-hoc text processing tasks — the things you do once, interactively, to get an answer quickly. For repetitive tasks, invest in a script. A weekly task of cleaning a CSV, deduplicating lines, normalizing case, and counting results is worth automating. A shell script that chains a few commands with pipes, or a Node.js/Python script that calls the same transformations programmatically, turns a 5-minute manual process into a sub-second automated one.</p><p>When building text processing into a production pipeline, consider idempotency. A deduplication step that runs twice should produce the same output both times. A case conversion step should be reversible or at least documented as lossy. A word count step should produce consistent results regardless of input order. Idempotent processing steps are composable and testable — you can run them repeatedly during development without accumulating side effects.</p>',

  "unix-timestamps-guide": '<h2>Timestamps in Databases and APIs</h2><p>Database timestamp handling varies by engine. MySQL\'s TIMESTAMP type is 32-bit and range-limited to 1970-2038; use DATETIME for dates outside that range. PostgreSQL\'s TIMESTAMP WITH TIME ZONE stores UTC internally and converts to the session\'s timezone on retrieval. SQLite has no native timestamp type — timestamps are stored as TEXT (ISO 8601 strings), INTEGER (Unix seconds), or REAL (Unix seconds with fractional part). Know your database, and test your timestamp handling with dates near epoch boundaries and DST transitions.</p><p>When designing an API, document the timestamp format explicitly. "created_at: 1700000000" is ambiguous without specifying "Unix timestamp in seconds." Better: created_at is an integer representing Unix timestamp in seconds (UTC). If your API returns timestamps in milliseconds, document it. If it returns ISO 8601 strings, specify whether they include timezone offsets. Ambiguity about timestamps in API documentation causes integration bugs that are hard to reproduce because they depend on the client\'s timezone and clock skew.</p>',

  "web-encoding-guide": '<h2>Encoding in Authentication and Security Contexts</h2><p>HTTP Basic Authentication uses Base64 to encode the credentials string username:password. The resulting header looks like Authorization: Basic dXNlcjpwYXNz. This is encoding, not encryption — anyone who intercepts the request can trivially decode the credentials. Basic Auth must always be sent over HTTPS. For API authentication, token-based methods (Bearer tokens, API keys) have largely replaced Basic Auth because they do not expose the user\'s password on every request.</p><p>JWTs (JSON Web Tokens) use Base64URL encoding for their header and payload sections. Base64URL is a variant of Base64 that replaces + with - and / with _ to make the encoded string safe for use in URLs without additional percent-encoding. The signature section of a JWT is also Base64URL-encoded, but the underlying data is binary (the output of the HMAC or RSA signing algorithm), not text. Decoding a JWT without verifying the signature reveals the header and payload but does not compromise security — JWTs are signed, not encrypted. The information in the payload is public to anyone who can see the token. Never put secrets in a JWT payload.</p>',

  "word-count-seo-guide": '<h2>Content Metrics Beyond Word Count</h2><p>Word count is a proxy for comprehensiveness, not a goal in itself. Other metrics matter more for user engagement and SEO: time on page (how long do readers stay?), bounce rate (do they leave immediately?), scroll depth (do they read to the end?), and conversion rate (do they take the desired action?). A 3,000-word article that readers bounce from after 15 seconds is performing worse than an 800-word article they read to the end. Use word count as one signal among many, not as the sole measure of content quality.</p><p>Readability scores (Flesch-Kincaid, Gunning Fog) estimate the education level needed to understand your text. For web content, aim for a reading level of 8th-10th grade. This does not mean "dumb down" your content — it means writing clearly. Short sentences. Active voice. Concrete examples. Technical terms explained when first used. These practices improve readability and comprehension at any education level. Hemingway App and Grammarly both provide readability feedback integrated into the writing process.</p>',
};

const guidesDir = "src/pages/guides";
let updated = 0;
for (const guideId in sections) {
  const f = path.join(guidesDir, guideId + ".astro");
  if (!fs.existsSync(f)) continue;
  let c = fs.readFileSync(f, "utf8");
  c = c.replace("\n</div>\n`", sections[guideId] + "\n</div>\n`");
  fs.writeFileSync(f, c);
  updated++;
}
console.log("Added large sections to " + updated + " guides");
